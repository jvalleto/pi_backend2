# Archivo de ejemplo para variables de entorno
# Copia este archivo como .env y completa con tus valores reales

# ===========================================
# CONFIGURACIÓN DE PERFILES
# ===========================================

# Perfil activo (dev, prod)
spring.profiles.active=prod

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS (PRODUCCIÓN)
# ===========================================

# Solo necesario para el perfil 'prod'
spring.datasource.password=12345
spring.datasource.username=//postgres.msumfbtkqazatpnyfnky
spring.datasource.url=jdbc:postgresql://@aws-0-us-east-1.pooler.supabase.com:5432/postgres/postgres?sslmode=require&connectTimeout=30&socketTimeout=30&loginTimeout=30
spring.datasource.driver-class-name=org.postgresql.Driver
# Hace que Hibernate cree/actualice la tabla automáticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===========================================
# CONFIGURACIÓN ADICIONAL (OPCIONAL)
# ===========================================

# Puerto del servidor (opcional, por defecto 8080)
server.port=8080

